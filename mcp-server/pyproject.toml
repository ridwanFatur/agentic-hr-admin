[project]
name = "mcp-server"
version = "0.1.0"
description = "FastMCP PostgreSQL server for HR Manager database"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "asyncpg>=0.31.0",
    "python-dotenv>=1.0.0",
    "uvicorn[standard]>=0.30.0",
]
